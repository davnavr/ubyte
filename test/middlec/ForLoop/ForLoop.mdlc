namespace ForLoop {

type Program {
    method Main entrypoint (argv char32[][]) -> (s32) {
        var counter s32 = 0;
        while (counter < argv.length) {
            counter <- counter + 1;
        }
        return counter;
    }
}

}
