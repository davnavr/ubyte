namespace Application {

using Library;

type Program {
    method Main entrypoint () -> () {
        var message char32[] = Messages::Greeting();
        Program::Print(message);
    }

    method Print private (message char32[]) -> () external {
        entry = "testhelperprintln",
        library = "runmdl"
    }
}

}
